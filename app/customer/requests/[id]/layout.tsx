export const dynamic = "force-dynamic";
export const runtime = "nodejs";
export const dynamicParams = true;

export default function CustomerRequestsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}